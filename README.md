# clog-sample

Minimal public example of a CLI app built with the [util](https://github.com/mrmxf/util) modules.

Demonstrates wiring together `util/kfg`, `util/buildinfo`, `util/check`, `util/snippets`, and
`util/gommi/bare` into a working Cobra CLI.
